import { defineMessages } from "react-intl";

export const translations = defineMessages({
  subvolumesLabel: {
    id: "components.subvolumeSelector.label",
    defaultMessage: "Selected subvolume"
  }
});
