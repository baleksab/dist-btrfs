import { defineMessages } from "react-intl";

export const translations = defineMessages({
  light: {
    id: "components.themeSelector.light",
    defaultMessage: "Light"
  },
  dark: {
    id: "components.themeSelector.dark",
    defaultMessage: "Dark"
  }
});
