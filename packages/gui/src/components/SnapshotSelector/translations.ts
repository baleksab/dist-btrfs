import { defineMessages } from "react-intl";

export const translations = defineMessages({
  selectedSnapshot: {
    id: "components.fullReplicationForm.selectedSnapshot",
    defaultMessage: "Selected snapshot"
  }
});
